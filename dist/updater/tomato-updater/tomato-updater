#!/bin/bash

apt-get update >/dev/null
for pkg in tomato-host-proxmox tomato-hostmanager tomato-repy tomato-updater; do
  if dpkg-query --status google-chrome-beta 2>/dev/null | fgrep -q installed; then
    apt-get install -y $pkg >/dev/null 2>&1
  fi
done
